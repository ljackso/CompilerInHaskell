
func main() {
    l = ack(3,3);
    Show(l);
    Print("it worked");
};

func ack(m int, n int) int {
    Show(n);
    Show(m);
    if (m == 0){
        return (n+1);
    };

    if (m > 0){
        if (n == 0) {
            return ack(m-1, 1);
        };
        
        if( n > 0) {
            return ack(m-1, ack(m, n -1));
        };
    };
};